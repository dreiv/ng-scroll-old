import { WindowScrollSpyDirective } from './window-scroll-spy.directive';

describe('WindowScrollSpyDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowScrollSpyDirective();
    expect(directive).toBeTruthy();
  });
});
